# Portfolio demo assets

Ensemble kinematics PNGs for the README are **on hold** — the current synthetic hip/knee/ankle plots share the same curve shape (amplitude-only differences). Use `plot-ensemble-kinematics` on real batch output for publication-style figures.

When a better demo is ready:

```bash
pip install -e .
python examples/generate_demo_ensemble_plots.py
```

Outputs (local, not currently embedded in README):

- `docs/assets/ensemble_hip_angle_demo.png`
- `docs/assets/ensemble_knee_angle_demo.png`
- `docs/assets/ensemble_ankle_angle_demo.png`
